name: 台股 AI 每日早報

on:
  push:
    branches:
      - main
  schedule:
    # 23:58 UTC(日一二三四) = 07:58 台灣時間(週一至週五)：更新網站資料與 GitHub Pages 檔案，不發 Telegram。
    # 避開 GitHub Actions 整點高峰，降低 schedule 被延遲或丟棄的機率。
    - cron: "58 23 * * 0-4"
    # 00:18 UTC = 08:18 台灣時間：只發 Telegram 早報。
    - cron: "18 0 * * 1-5"
  workflow_dispatch:
    inputs:
      send_telegram:
        description: "手動執行時是否也發送 Telegram"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

concurrency:
  group: tw-stock-ai-daily
  cancel-in-progress: false

jobs:
  dashboard-update:
    name: 07:58 update dashboard
    if: >-
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch' ||
      github.event.schedule == '58 23 * * 0-4'
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate dashboard without Telegram
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python main.py --dry-run --telegram-summary

      - name: Commit and push dashboard
        run: |
          mkdir -p docs
          cp -f dashboard/index.html docs/index.html
          cp -f dashboard/dashboard_data.json docs/dashboard_data.json
          cp -f dashboard/performance.html docs/performance.html
          cp -f dashboard/performance_data.json docs/performance_data.json
          cp -f dashboard/theme_history.json docs/theme_history.json
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dashboard/
          git add docs/
          git add data/tw_stock_ai.sqlite3 || true
          git diff --cached --quiet || git commit -m "auto: dashboard $(date -u +'%Y-%m-%d')"
          git push

      - name: Notify dashboard failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="台股 AI 07:58 網站更新失敗，請檢查 GitHub Actions。"
          fi

  morning-telegram:
    name: 08:18 send Telegram
    if: >-
      github.event.schedule == '18 0 * * 1-5' ||
      (github.event_name == 'workflow_dispatch' && inputs.send_telegram == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Send Telegram morning report
        env:
          FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python main.py --send-telegram --telegram-summary

      - name: Notify failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="台股 AI 08:18 Telegram 早報失敗，請檢查 GitHub Actions。"
          fi
